# Terraform working directory
.terraform/

# State files (NEVER COMMIT)
*.tfstate
*.tfstate.*
terraform.tfstate.d/

# Variables (may contain secrets)
*.tfvars
terraform.tfvars

# Plan files
tfplan

# Crash / backup files
*.backup

# Lock file (safe to commit, but optional choice)
.terraform.lock.hcl